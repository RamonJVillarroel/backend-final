/* const fs = require('fs');
const dataProductos = (products)=>{
    fs.writeFileSync('./dataBase/data.json', JSON.stringify(products, null, 2), { encoding:'utf-8',});    
    return 'ok';
};
const dataCarrito= (carrito)=>{
    fs.writeFileSync('./dataBase/carrito.json', JSON.stringify(carrito, null, 2), {
        encoding:'utf-8',
    });
};
module.exports={
    dataCarrito,
    dataProductos,
}; */